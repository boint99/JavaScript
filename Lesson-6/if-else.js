/**
=> câu điều kiện
If - else: Nếu A thì làm gì, không phải A thì làm gì
If - else if Nếu A thì không phải A thì A lại nếu B, ko phải B thì nữa thì...vvv

*/
let weather = 'HOT'
if (weather === 'HOT') {
    console.log('OK')
}else {
console.log('NOK')
}